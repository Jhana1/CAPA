int main()
{
    int *a, *b, *c;
    int i, j, k;

    for (i = 0; i < j; ++i)
    {
        k += a[i];
    }

    for (i = 0; i < j; ++i)
    {
        k = k + a[i];
    }

    for (i = 0; i < j; ++i)
    {
        k /= k + a[i];
    }

    return 0;
}

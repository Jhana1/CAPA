Quick Start
-------------------------------------------------------------------------------

./make - Delete existing build, check out dependencies, build LLVM/Clang,
         build CAPA in optimized mode, bundle binaries and libraries
         together, and finally archive everything at ../build folder


